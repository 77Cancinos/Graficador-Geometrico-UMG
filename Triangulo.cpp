
#include "Triangulo.h"
#include <iostream>
#include <cmath>

void Triangulo::dibujar() {
    int height = static_cast<int>((sqrt(3) / 2) * baseSize);

    for (int y = 0; y < height; ++y) {
        // Implementación similar a la función drawTriangle anteriormente mencionada
    }
}
